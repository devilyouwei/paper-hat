[project]
name = "hat-paper-workflow"
version = "0.1.0"
description = "Lightweight research paper workflow agent (search, download, metadata, citation graph)"
readme = "readme.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.31",
    "pyvis>=0.3.2",
    "pypdf>=4.0",
]

[tool.uv]
package = false
