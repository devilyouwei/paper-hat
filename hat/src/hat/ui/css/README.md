# `ui/css/` — stylesheets

Plain CSS for the vanilla web app — no preprocessor, no build step.

## Files

| File | Purpose |
| --- | --- |
| `style.css` | Single global stylesheet for layout, the three-tab shell (Chat / Models / Memory), chat bubbles, the streaming `<think>…</think>` aside, and the trace-lifecycle panel. |
